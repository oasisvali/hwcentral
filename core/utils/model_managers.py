# Adding extra manager methods is the preferred way to add “table-level” functionality to your models. (For “row-level”
# functionality – i.e., functions that act on a single instance of a model object use model methods

# E.G. objects is the default model manager - Submission.objects.all()
